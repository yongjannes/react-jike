//统一中转工具模块函数
//import {http} from '@/utils'

import {http} from './request'
import {setToken,getToken,clearToken} from './token'
export {
    http,
    setToken,
    getToken,
    clearToken
}